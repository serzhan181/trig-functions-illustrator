import { Pos } from "./main";
import colors from "tailwindcss/colors";

export const CIRCLE_RAD = 350;
export const CENTER: Pos = {
  x: 1280 / 2,
  y: 800 / 2,
};

export const PLAY_SVG_PATH = "/svgs/play.svg";
export const PAUSE_SVG_PATH = "/svgs/pause.svg";

export const SIN_COLOR = colors.red[600];
export const COS_COLOR = colors.blue[600];
export const PRIMARY_COLOR = colors.neutral[900];
export const SECONDARY_COLOR = colors.orange[600];
